The "TestResources >> handles" group contains various assets for Basic Handles test suite.

They are as empty as GameMaker permits. Their main purpose is to check if they work correctly with handles-related functions, rather than testing functionalities specific to their asset type.
