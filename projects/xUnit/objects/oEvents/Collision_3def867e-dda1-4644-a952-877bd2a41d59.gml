assert_areEqual(event_number, 0, "Event number was correct for the collision event with oTest");
assert_areEqual(event_object, global.expectedIndex, "Event object was correct for the collision event with oTest");
assert_areEqual(event_type, ev_collision, "Event type was correct for the collision event with oTest");