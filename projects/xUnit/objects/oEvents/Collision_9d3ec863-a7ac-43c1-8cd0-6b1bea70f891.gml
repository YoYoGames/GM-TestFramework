assert_areEqual(event_number, 1, "Event number was correct for the collision event with oOther");
assert_areEqual(event_object, global.expectedIndex, "Event object was correct for the collision event with oOther");
assert_areEqual(event_type, ev_collision, "Event type was correct for the collision event with oOther");